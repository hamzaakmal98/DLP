embedding_dim: 8
num_experts: 2
expert_hidden_size: 32
tower_hidden_size: 16
loss_weights:
  like: 1.0
  long_view: 1.0
  creator_interest: 1.0
batch_size: 256
learning_rate: 0.001
epochs: 1
creator_task_enabled: false
topk: [100]
checkpoint_dir: artifacts/models
forbidden_target_columns: [is_like, is_click, long_view, creator_interest]
